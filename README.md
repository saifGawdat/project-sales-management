# نظام إدارة المخزون والمبيعات

نظام شامل لإدارة المخزون والمبيعات مبني باستخدام React و Tailwind CSS.

## 🚀 المميزات

- ✅ **واجهة مستخدم حديثة ومتجاوبة** - تصميم mobile-first يعمل على جميع الأجهزة
- ✅ **نظام مصادقة كامل** - تسجيل دخول آمن مع حماية المسارات
- ✅ **إدارة الفئات** - إضافة وتعديل وحذف الفئات
- ✅ **إدارة أنواع المنتجات** - تنظيم أنواع المنتجات المختلفة
- ✅ **إدارة المنتجات** - إدارة كاملة للمنتجات مع ربطها بالفئات والأنواع
- ✅ **إدارة المستودع** - تتبع الكميات والمواقع في المستودع
- ✅ **إدارة السيارات** - نظام عمل داخلي لإدارة السيارات
- ✅ **إدارة المبيعات** - تسجيل وإدارة الطلبات والمبيعات
- ✅ **إدارة المصروفات** - تتبع جميع المصروفات
- ✅ **ملخص الأرباح** - تقارير شاملة للأرباح والخسائر

## 📦 التكنولوجيات المستخدمة

- **React 19** - مكتبة JavaScript لبناء واجهات المستخدم
- **React Router DOM** - إدارة التنقل بين الصفحات
- **Tailwind CSS** - إطار عمل CSS للتصميم السريع
- **Axios** - مكتبة HTTP للتواصل مع API
- **GSAP** - مكتبة الرسوم المتحركة
- **React Hook Form** - إدارة النماذج
- **Zod** - التحقق من صحة البيانات

## 📁 هيكل المشروع

```
src/
├── components/          # المكونات القابلة لإعادة الاستخدام
│   ├── layouts/        # مكونات التخطيط (Sidebar, Header, DashboardLayout)
│   └── ui/             # مكونات واجهة المستخدم (Button, Input, Modal, Table, Loading)
├── contexts/           # سياقات React (AuthContext)
├── pages/              # صفحات التطبيق
│   ├── Auth/           # صفحات المصادقة
│   ├── Categories/     # صفحات الفئات
│   ├── ProductTypes/   # صفحات أنواع المنتجات
│   ├── Products/       # صفحات المنتجات
│   ├── Warehouse/      # صفحات المستودع
│   ├── Cars/           # صفحات السيارات
│   ├── Sales/          # صفحات المبيعات
│   ├── Expenses/       # صفحات المصروفات
│   └── Profit/         # صفحات ملخص الأرباح
├── services/           # خدمات API
│   └── api.js         # جميع طلبات HTTP
├── App.jsx            # الملف الرئيسي للتطبيق مع التوجيهات
└── main.jsx           # نقطة الدخول للتطبيق
```

## 🔧 التثبيت والتشغيل

1. **تثبيت الحزم:**
```bash
npm install
```

2. **تشغيل المشروع في وضع التطوير:**
```bash
npm run dev
```

3. **بناء المشروع للإنتاج:**
```bash
npm run build
```

## 🔐 نظام المصادقة

يستخدم التطبيق نظام مصادقة يعتمد على JWT tokens:
- يتم حفظ الـ token في `localStorage` بعد تسجيل الدخول
- يتم إرفاق الـ token تلقائياً في جميع طلبات API
- في حالة انتهاء صلاحية الـ token (401)، يتم توجيه المستخدم تلقائياً لصفحة تسجيل الدخول

## 📡 API Integration

جميع طلبات API موجودة في `src/services/api.js`:
- **Base URL**: `http://autopartsdemo.runasp.net/api`
- يتم إضافة الـ token تلقائياً في header كل طلب
- جميع الطلبات تستخدم Axios مع interceptors للتعامل مع الأخطاء

### Endpoints المستخدمة:

- `/Auth/login` - تسجيل الدخول
- `/Categories` - إدارة الفئات
- `/ProductTypes` - إدارة أنواع المنتجات
- `/Products` - إدارة المنتجات
- `/Warehouse` - إدارة المستودع
- `/Cars` - إدارة السيارات
- `/Sales` - إدارة المبيعات
- `/Expenses` - إدارة المصروفات
- `/Profit/summary` - ملخص الأرباح

## 🎨 المكونات الرئيسية

### 1. AuthContext (`src/contexts/AuthContext.jsx`)
- إدارة حالة المصادقة في التطبيق
- دوال تسجيل الدخول والخروج
- التحقق من حالة المصادقة

### 2. ProtectedRoute (`src/components/ProtectedRoute.jsx`)
- حماية المسارات التي تتطلب تسجيل دخول
- إعادة التوجيه تلقائياً لصفحة تسجيل الدخول إذا لم يكن المستخدم مسجل دخول

### 3. DashboardLayout (`src/components/layouts/DashboardLayout.jsx`)
- التخطيط الرئيسي للتطبيق
- يحتوي على Sidebar و Header
- متجاوب مع جميع أحجام الشاشات

### 4. UI Components
- **Button**: زر قابل لإعادة الاستخدام مع أنماط مختلفة
- **Input**: حقل إدخال مع دعم الأخطاء
- **Modal**: نافذة منبثقة للنماذج
- **Table**: جدول لعرض البيانات مع دعم التعديل والحذف
- **Loading**: مؤشر تحميل

## 🔄 تدفق البيانات

1. **المستخدم يسجل الدخول** → يتم حفظ الـ token في localStorage
2. **الوصول لصفحة محمية** → ProtectedRoute يتحقق من المصادقة
3. **جلب البيانات** → استخدام API services لجلب البيانات من الخادم
4. **عرض البيانات** → استخدام Table component لعرض البيانات
5. **التعديل/الحذف** → استخدام Modal component مع النماذج
6. **حفظ التغييرات** → إرسال طلب API ثم تحديث البيانات

## ✨ الرسوم المتحركة

يستخدم التطبيق GSAP لإضافة رسوم متحركة سلسة:
- ظهور الصفحات تدريجياً عند التحميل
- انتقالات النوافذ المنبثقة
- تأثيرات hover على العناصر التفاعلية

## 📱 التصميم المتجاوب

- **Mobile First**: التصميم يبدأ من الجوال ثم يتوسع للشاشات الكبيرة
- **Sidebar**: يتحول لقائمة منبثقة على الجوال
- **Tables**: قابلة للتمرير الأفقي على الشاشات الصغيرة
- **Forms**: تتكيف مع حجم الشاشة

## 🛠️ التحسينات المستقبلية

- [ ] إضافة نظام البحث والفلترة المتقدم
- [ ] إضافة تصدير التقارير (PDF, Excel)
- [ ] إضافة إشعارات للمستخدم
- [ ] إضافة نظام الصلاحيات (Roles)
- [ ] إضافة Dashboard مع إحصائيات مرئية
- [ ] تحسين الأداء باستخدام React.memo و useMemo
- [ ] إضافة Unit Tests
- [ ] إضافة Dark Mode

## 📝 ملاحظات

- تأكد من أن API الخلفي يعمل على `http://autopartsdemo.runasp.net`
- جميع النماذج تحتوي على تحقق من البيانات
- يتم التعامل مع حالات الخطأ والتحميل في جميع الصفحات
- الكود منظم بشكل modular وسهل الصيانة

## 👨‍💻 المطور

تم تطوير هذا المشروع كواجهة أمامية كاملة لنظام إدارة المخزون والمبيعات.

---

**ملاحظة**: تأكد من تحديث `API_BASE_URL` في `src/services/api.js` إذا تغير عنوان API الخلفي.
