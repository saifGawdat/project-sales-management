# توثيق المشروع - نظام إدارة المخزون والمبيعات

## 📋 نظرة عامة

هذا المشروع هو واجهة أمامية كاملة لنظام إدارة المخزون والمبيعات، مبني باستخدام React و Tailwind CSS. يوفر النظام إدارة شاملة لجميع جوانب العمل من الفئات والمنتجات إلى المبيعات والمصروفات.

---

## 🏗️ المكونات الرئيسية وشرحها

### 1. نظام المصادقة (Authentication System)

#### AuthContext (`src/contexts/AuthContext.jsx`)
**الوظيفة**: إدارة حالة المصادقة في التطبيق بالكامل.

**كيف يعمل**:
- يحفظ معلومات المستخدم و token في state
- يوفر دوال `login` و `logout` لجميع المكونات
- يتحقق تلقائياً من حالة المصادقة عند تحميل التطبيق
- يحفظ token في `localStorage` للاحتفاظ بحالة تسجيل الدخول

**تدفق البيانات**:
```
المستخدم يدخل بيانات تسجيل الدخول 
  ↓
دالة login() ترسل طلب API
  ↓
في حالة النجاح: حفظ token في localStorage
  ↓
تحديث state (user, isAuthenticated)
  ↓
إعادة توجيه المستخدم للصفحة الرئيسية
```

#### ProtectedRoute (`src/components/ProtectedRoute.jsx`)
**الوظيفة**: حماية المسارات التي تتطلب تسجيل دخول.

**كيف يعمل**:
- يستخدم `useAuth()` للتحقق من حالة المصادقة
- إذا لم يكن المستخدم مسجل دخول، يعيد توجيهه لصفحة تسجيل الدخول
- يعرض مؤشر تحميل أثناء التحقق من المصادقة

---

### 2. خدمات API (API Services)

#### api.js (`src/services/api.js`)
**الوظيفة**: نقطة اتصال واحدة مع API الخلفي.

**المكونات**:
- **axios instance**: إعدادات افتراضية لجميع الطلبات
- **interceptors**: 
  - Request interceptor: يضيف token تلقائياً لكل طلب
  - Response interceptor: يتعامل مع أخطاء 401 (غير مصرح)

**تدفق الطلب**:
```
المكون يطلب بيانات
  ↓
استدعاء دالة من API service (مثلاً: categoriesAPI.getAll())
  ↓
Request interceptor يضيف token للـ header
  ↓
إرسال الطلب للخادم
  ↓
Response interceptor يتحقق من النتيجة
  ↓
في حالة 401: حذف token وإعادة توجيه لتسجيل الدخول
  ↓
إرجاع البيانات للمكون
```

---

### 3. مكونات التخطيط (Layout Components)

#### DashboardLayout (`src/components/layouts/DashboardLayout.jsx`)
**الوظيفة**: التخطيط الرئيسي للتطبيق.

**المكونات**:
- **Sidebar**: قائمة التنقل الجانبية
- **Header**: رأس الصفحة مع البحث ومعلومات المستخدم
- **Outlet**: مكان عرض محتوى الصفحات

**كيف يعمل**:
- يستخدم `useState` لإدارة حالة فتح/إغلاق Sidebar على الجوال
- يوفر تخطيط متجاوب يعمل على جميع الأجهزة

#### Sidebar (`src/components/layouts/Sidebar.jsx`)
**الوظيفة**: قائمة التنقل الرئيسية.

**المميزات**:
- قائمة بجميع صفحات التطبيق
- استخدام `NavLink` من React Router لتحديد الصفحة النشطة
- زر تسجيل الخروج
- متجاوب: يتحول لقائمة منبثقة على الجوال

#### Header (`src/components/layouts/Header.jsx`)
**الوظيفة**: رأس الصفحة مع أدوات التحكم.

**المكونات**:
- زر القائمة (للجوال)
- شريط البحث
- معلومات المستخدم الحالي

---

### 4. مكونات واجهة المستخدم (UI Components)

#### Button (`src/components/ui/Button.jsx`)
**الوظيفة**: زر قابل لإعادة الاستخدام.

**الخصائص**:
- `variant`: نوع الزر (primary, secondary, danger, success, outline)
- `size`: حجم الزر (sm, md, lg)
- دعم `disabled` state
- أنماط focus للوصولية

#### Input (`src/components/ui/Input.jsx`)
**الوظيفة**: حقل إدخال مع دعم الأخطاء.

**المميزات**:
- دعم label اختياري
- عرض رسائل الخطأ
- أنماط focus

#### Modal (`src/components/ui/Modal.jsx`)
**الوظيفة**: نافذة منبثقة للنماذج.

**المميزات**:
- رسوم متحركة باستخدام GSAP
- أحجام مختلفة (sm, md, lg, xl)
- Overlay قابل للنقر للإغلاق
- منع إغلاق عند النقر داخل النافذة

#### Table (`src/components/ui/Table.jsx`)
**الوظيفة**: جدول لعرض البيانات.

**المميزات**:
- دعم أعمدة مخصصة مع render functions
- أزرار تعديل وحذف
- حالات التحميل والفارغة
- تصميم متجاوب

#### Loading (`src/components/ui/Loading.jsx`)
**الوظيفة**: مؤشر تحميل.

---

### 5. صفحات التطبيق (Pages)

جميع الصفحات تتبع نفس النمط:

#### مثال: Categories (`src/pages/Categories/Categories.jsx`)

**الحالة (State)**:
```javascript
const [categories, setCategories] = useState([]);      // البيانات
const [loading, setLoading] = useState(true);            // حالة التحميل
const [isModalOpen, setIsModalOpen] = useState(false);  // حالة النافذة المنبثقة
const [editingCategory, setEditingCategory] = useState(null); // العنصر قيد التعديل
const [formData, setFormData] = useState({...});         // بيانات النموذج
```

**تدفق البيانات**:

1. **عند تحميل الصفحة**:
   ```
   useEffect → fetchCategories()
     ↓
   categoriesAPI.getAll()
     ↓
   تحديث state (categories, loading)
     ↓
   عرض البيانات في Table
   ```

2. **عند إضافة عنصر جديد**:
   ```
   المستخدم ينقر "إضافة"
     ↓
   handleAdd() → فتح Modal مع formData فارغ
     ↓
   المستخدم يملأ النموذج
     ↓
   handleSave() → categoriesAPI.create(formData)
     ↓
   إغلاق Modal → fetchCategories() لتحديث البيانات
   ```

3. **عند التعديل**:
   ```
   المستخدم ينقر "تعديل"
     ↓
   handleEdit(item) → تعبئة formData بالبيانات الحالية
     ↓
   فتح Modal مع البيانات
     ↓
   المستخدم يعدل البيانات
     ↓
   handleSave() → categoriesAPI.update(id, formData)
     ↓
   إغلاق Modal → fetchCategories()
   ```

4. **عند الحذف**:
   ```
   المستخدم ينقر "حذف"
     ↓
   تأكيد من المستخدم
     ↓
   categoriesAPI.delete(id)
     ↓
   fetchCategories() لتحديث البيانات
   ```

---

### 6. صفحة ملخص الأرباح (Profit Summary)

**الوظيفة**: حساب وتقديم تقرير شامل عن الأرباح.

**كيف يعمل**:
1. جلب بيانات المبيعات والمصروفات
2. تصفية البيانات حسب النطاق الزمني المحدد
3. حساب:
   - إجمالي المبيعات
   - إجمالي المصروفات
   - صافي الربح (المبيعات - المصروفات)
   - نسبة الربح
4. عرض النتائج في بطاقات إحصائية

**تدفق البيانات**:
```
المستخدم يحدد نطاق زمني
  ↓
fetchSummary()
  ↓
جلب المبيعات والمصروفات من API
  ↓
تصفية البيانات حسب التاريخ
  ↓
حساب الإجماليات والنسب
  ↓
عرض النتائج في StatCard components
```

---

## 🔄 تدفق البيانات العام في التطبيق

### 1. تدفق المصادقة
```
صفحة Login
  ↓
إدخال بيانات المستخدم
  ↓
authAPI.login(credentials)
  ↓
حفظ token في localStorage
  ↓
تحديث AuthContext
  ↓
إعادة توجيه للصفحة الرئيسية
```

### 2. تدفق جلب البيانات
```
المستخدم يفتح صفحة (مثلاً: Categories)
  ↓
ProtectedRoute يتحقق من المصادقة
  ↓
DashboardLayout يعرض التخطيط
  ↓
صفحة Categories تحمل
  ↓
useEffect → fetchCategories()
  ↓
categoriesAPI.getAll()
  ↓
Request interceptor يضيف token
  ↓
الخادم يرد بالبيانات
  ↓
تحديث state
  ↓
عرض البيانات في Table
```

### 3. تدفق إضافة/تعديل البيانات
```
المستخدم ينقر "إضافة" أو "تعديل"
  ↓
فتح Modal مع النموذج
  ↓
المستخدم يملأ/يعدل البيانات
  ↓
handleSave() → API call
  ↓
Request interceptor يضيف token
  ↓
الخادم يحفظ البيانات
  ↓
إغلاق Modal
  ↓
إعادة جلب البيانات لتحديث العرض
```

---

## 🎨 الرسوم المتحركة (GSAP Animations)

### استخدام GSAP في المشروع:

1. **في الصفحات**:
```javascript
useEffect(() => {
  gsap.fromTo(
    containerRef.current,
    { opacity: 0, y: 20 },  // الحالة الأولية
    { opacity: 1, y: 0, duration: 0.5 }  // الحالة النهائية
  );
}, []);
```
**النتيجة**: ظهور الصفحة تدريجياً من الأسفل مع fade in

2. **في Modal**:
```javascript
useEffect(() => {
  if (isOpen) {
    gsap.fromTo(overlayRef.current, { opacity: 0 }, { opacity: 1, duration: 0.3 });
    gsap.fromTo(
      modalRef.current,
      { opacity: 0, y: -20, scale: 0.95 },
      { opacity: 1, y: 0, scale: 1, duration: 0.3 }
    );
  }
}, [isOpen]);
```
**النتيجة**: ظهور النافذة المنبثقة مع تأثير scale و fade

---

## 📱 التصميم المتجاوب

### Mobile First Approach:
1. **الشاشات الصغيرة (< 1024px)**:
   - Sidebar يتحول لقائمة منبثقة
   - Tables قابلة للتمرير الأفقي
   - Forms تأخذ عرض كامل
   - Grids تتحول لعمود واحد

2. **الشاشات الكبيرة (≥ 1024px)**:
   - Sidebar ثابت على الجانب
   - Tables بعرض كامل
   - Forms في grid من عمودين
   - استخدام `lg:` prefix في Tailwind

---

## 🔐 الأمان

1. **حماية المسارات**: جميع الصفحات محمية بـ ProtectedRoute
2. **Token Management**: Token محفوظ في localStorage ويضاف تلقائياً للطلبات
3. **Auto Logout**: عند انتهاء صلاحية token (401)، يتم تسجيل الخروج تلقائياً
4. **Input Validation**: جميع النماذج تحتوي على تحقق من البيانات

---

## 🚀 تحسينات الأداء

1. **Lazy Loading**: يمكن إضافة lazy loading للصفحات
2. **Memoization**: يمكن استخدام React.memo و useMemo لتقليل إعادة الرسم
3. **Code Splitting**: يمكن تقسيم الكود حسب المسارات
4. **API Caching**: يمكن إضافة cache للبيانات التي لا تتغير كثيراً

---

## 📝 ملاحظات مهمة

1. **API Base URL**: موجود في `src/services/api.js` - يمكن تغييره حسب الحاجة
2. **Error Handling**: جميع الطلبات تحتوي على try-catch للتعامل مع الأخطاء
3. **Loading States**: جميع الصفحات تعرض حالات تحميل
4. **Empty States**: الجداول تعرض رسالة عند عدم وجود بيانات
5. **RTL Support**: التطبيق يدعم اللغة العربية مع RTL

---

## 🎯 الخلاصة

هذا المشروع يتبع أفضل الممارسات في تطوير React:
- ✅ بنية منظمة وواضحة
- ✅ مكونات قابلة لإعادة الاستخدام
- ✅ فصل الاهتمامات (separation of concerns)
- ✅ إدارة حالة مركزية (Context API)
- ✅ تصميم متجاوب
- ✅ تجربة مستخدم ممتازة
- ✅ كود نظيف مع تعليقات بالعربية

جميع المكونات موثقة بالعربية لتسهيل الفهم والصيانة.

